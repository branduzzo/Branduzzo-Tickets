## Packages
framer-motion | Page animations and smooth layout transitions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes efficiently

## Notes
The /api/generate endpoint returns a file (blob). The frontend must handle the blob response to trigger a browser download for 'main.py'.
Base fonts: 'Outfit' for headings, 'Inter' for body text.
